"""charpaste — type clipboard contents character-by-character.

A cross-platform (Linux/Windows) tray app that mimics the AutoHotkey
KaseyaPaste script: instead of Ctrl+V, it *types* the clipboard text one
character at a time, so it works in clipboard-disabled remote sessions
(AVD, Citrix, locked-down RDP, etc.).
"""

__version__ = "0.1.0"
